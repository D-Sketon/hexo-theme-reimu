<%_ if (post.categories && post.categories.length) { _%>
  <%- listCategories(post.categories, {
    show_count: false,
    class: 'article-category',
    style: 'none',
    separator: ''
  }, theme.animation.options.article.category) %>
<%_ } _%>
