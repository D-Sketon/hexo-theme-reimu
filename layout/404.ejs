<!DOCTYPE html>
<html <% if (config.language) { %>lang="<%- config.language %>"
        <% } %>
      <% if (theme.dark_mode.enable) { %>data-theme="dark"
        <% } %>
>
<%- partial('_partial/head') %>
<body>
<div id="container">
    <div id="wrap">
        <%- partial('_partial/header', {post: page}, {cache: !config.relative_link}) %>
    </div>
    <%- partial('_partial/after-footer') %>
</div>
</body>
</html>
